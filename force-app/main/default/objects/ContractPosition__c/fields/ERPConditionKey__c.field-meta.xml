<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ERPConditionKey__c</fullName>
    <caseSensitive>false</caseSensitive>
    <description>Technical upsert key of the interface: SAP agreement number plus condition sequence number. Ensures the nightly sync finds each condition line reliably instead of overwriting or duplicating positions. [Sources: D-01 2026-08-11, B-01]</description>
    <externalId>true</externalId>
    <inlineHelpText>Technical upsert key of the interface: SAP agreement number plus condition sequence number. Ensures the nightly sync finds each condition line reliably instead of overwriting or duplicating positions.</inlineHelpText>
    <label>ERP Condition Key</label>
    <length>30</length>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>true</unique>
</CustomField>
