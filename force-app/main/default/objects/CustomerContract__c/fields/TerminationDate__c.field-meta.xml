<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>TerminationDate__c</fullName>
    <description>Termination date from SAP. Important: it does NOT automatically change the contract status in SAP — a termination is only visible via this field. [Sources: Confluence 31 Vertrag abschließen]</description>
    <inlineHelpText>Termination date from SAP. Important: it does NOT automatically change the contract status in SAP — a termination is only visible via this field.</inlineHelpText>
    <label>Termination Date</label>
    <required>false</required>
    <trackHistory>true</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
