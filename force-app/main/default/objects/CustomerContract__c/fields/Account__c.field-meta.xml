<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account__c</fullName>
    <deleteConstraint>Restrict</deleteConstraint>
    <description>The customer this contract belongs to. There is exactly one active contract per customer — all agreements (index, refund, marketing subsidy) hang below it as positions. [Sources: MN-001 Z.35]</description>
    <inlineHelpText>The customer this contract belongs to. There is exactly one active contract per customer — all agreements (index, refund, marketing subsidy) hang below it as positions.</inlineHelpText>
    <label>Account</label>
    <referenceTo>Account</referenceTo>
    <relationshipLabel>Customer Contracts</relationshipLabel>
    <relationshipName>CustomerContracts</relationshipName>
    <required>true</required>
    <trackHistory>true</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
