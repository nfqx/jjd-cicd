<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ContractPosition__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Only set when the causation party applies to a single agreement (SAP agreement level) instead of the whole contract. [Sources: D-02]</description>
    <inlineHelpText>Only set when the causation party applies to a single agreement (SAP agreement level) instead of the whole contract.</inlineHelpText>
    <label>Contract Position</label>
    <referenceTo>ContractPosition__c</referenceTo>
    <relationshipLabel>Causation Parties</relationshipLabel>
    <relationshipName>CausationParties</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
