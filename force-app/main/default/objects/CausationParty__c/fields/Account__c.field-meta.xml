<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>The customer/debtor whose purchases count — resolved via the SAP customer number. May stay empty if the number cannot (yet) be matched to an account. [Sources: D-02]</description>
    <inlineHelpText>The customer/debtor whose purchases count — resolved via the SAP customer number. May stay empty if the number cannot (yet) be matched to an account.</inlineHelpText>
    <label>Account</label>
    <referenceTo>Account</referenceTo>
    <relationshipLabel>Causation Parties</relationshipLabel>
    <relationshipName>CausationParties</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
